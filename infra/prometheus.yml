global:
  scrape_interval: 5s

rule_files:
  - /etc/prometheus/rules.yml

scrape_configs:
  - job_name: keyservice
    metrics_path: /metrics
    dns_sd_configs:
      - names: ["keyservice"]
        type: A
        port: 8080
